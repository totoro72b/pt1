import item_36_use_subprocess_for_child_processes

# no tests for this one, as just manually tested out/played with subprocess
# guess i could write tests to test the exit status, but that would be the same as the existing code
# so just run item_36.py instead
